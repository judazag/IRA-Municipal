import pandas as pd; from pathlib import Path; p=Path('data/04_model_output/ira_municipal_final.parquet'); print('exists', p.exists()); df=pd.read_parquet(p); print('rows', len(df)); print('cols', list(df.columns)); cols=['divipola','municipio','ira_score','ira_level','ira_riesgo','nivel_ira','nivel_predicho']; print('found', [c for c in cols if c in df.columns]); print(df[[c for c in cols if c in df.columns]].head(10).to_dict('records'))
